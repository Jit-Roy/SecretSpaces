package com.secretspaces32.android.ui.theme

import androidx.compose.ui.graphics.Color

// Primary gradient colors - Coral/Red theme
val TealPrimary = Color(0xFFE85D75)  // Coral red from the image
val TealLight = Color(0xFFFF8BA0)    // Lighter coral
val TealDark = Color(0xFFB83D55)     // Darker coral
val AquaGreen = Color(0xFFE85D75)    // Match primary
val SoftTeal = Color(0xFFFF6B85)     // Soft coral
val LightTeal = Color(0xFFFFB3C1)    // Light coral

// Accent colors
val DeepPurple = Color(0xFFE85D75)   // Match primary coral
val ElectricBlue = Color(0xFFFF6B85) // Coral variant
val CoralPink = Color(0xFFE85D75)    // Primary coral
val SoftBlue = Color(0xFFFF8BA0)     // Light coral
val LightBlue = Color(0xFFFFB3C1)    // Very light coral
val LightPink = Color(0xFFFFD4DD)    // Pale coral
val LavenderMist = Color(0xFFFF8BA0) // Light coral

// Neutral colors - Black background theme
val DarkBackground = Color(0xFF000000)  // Pure black from image
val DarkSurface = Color(0xFF1A1625)     // Dark purple-gray cards
val DarkCard = Color(0xFF221E2E)        // Slightly lighter cards
val DarkElevated = Color(0xFF2A2538)    // Elevated surfaces
val CharcoalGray = Color(0xFF3A3544)    // Gray accents
val DeepGray = Color(0xFF151320)        // Deep gray
val Silver = Color(0xFF8A8594)          // Muted text
val Platinum = Color(0xFFE5E5E5)        // White text
val LightGray = Color(0xFFE0E0E0)
val MediumGray = Color(0xFF6B6876)      // Medium gray for secondary text

// Semantic colors
val SuccessGreen = Color(0xFF00E676)
val ErrorRed = Color(0xFFE85D75)        // Use coral for errors too
val WarningOrange = Color(0xFFFFAB40)
